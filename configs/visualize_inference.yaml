defaults:
  - hardware: default
  - _self_

visualize:
  checkpoint: ???
  input_dir: ???
  output_dir: ???
  direction: AB
  num_images: null

hydra:
  run:
    dir: .
  output_subdir: null
  job_logging:
    root:
      handlers: []
